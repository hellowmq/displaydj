# 后续里程碑

这些是待完成工作，不是当前能力声明。

1. **真实硬件验收**：指定显示器，记录 UUID / 连接方式 / 原始亮度；验证读写与回读、失败恢复、断开/重连、睡眠唤醒。恢复到原设置后才记录通过。
2. **单一控制服务**：菜单栏亮度和 CLI 全部通过一个服务处理；解决用户手动修改与 Agent 快照恢复的所有权冲突，并提供可见的暂停 Agent 控制。
3. **菜单栏软件调光设置（TODO）**：参考成熟应用的显式设置，为外接显示器提供可选择的软件 Gamma 调光模式、启停开关和当前 transport 状态；DDC 失败时不能静默切换，必须由用户选择或确认。内建屏 / 外屏 / Gamma 的能力差异应可见，不能把软件变暗标成硬件背光。
4. **并发和状态持久化**：迁移 VibeDisplay 到 Swift 6 严格并发；串行化状态事务，记录快照使用的 transport，恢复时拒绝跨 transport 猜测。
5. **可靠分发**：在现有 GitHub Actions 基础上补齐真实硬件矩阵，再做双架构、Developer ID 签名、公证与正式 Release。
